为了测试文件读出后写入 dut ，再将结果写入输出文件，建一个简单的加法模块来测试一下。

---

由于系统任务是在仿真时使用，所以这里全是仿真代码。



仿真中的文件当前目录是 <>.sim\sim_1\behav\xsim\，所以如果输出文件设为 "."，就会被输出到这里，输入文件最好不要放在这里，因为在 Vivado 中复位仿真时，会把这个文件夹全部清除，可以放在上一级目录中。







